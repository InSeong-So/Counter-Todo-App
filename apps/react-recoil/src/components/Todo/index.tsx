import TodoInput from './TodoInput';
import TodoDisplay from './TodoDisplay';

const Todo = () => {
  return (
    <section id="todo-area">
      <TodoInput />
      <TodoDisplay />
    </section>
  );
};

export default Todo;
